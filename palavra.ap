prog ads2a;

    inteiro t;
    booleano ret;
    inteiro x;
    const inteiro a = t;

    func inteiro teste(inteiro a, inteiro b){
        inteiro ret;
        inteiro x;
        se(a > b e b > 10){
            a = nao a > b e b >= 10;
        }senao{ 
            retorno b; 
        }
        
        ret = a + b * 10 + 2 / 2;
        retorno ret; 
    }

    func vazio procedi(inteiro a) {
        inteiro c;
        a = c;
    }

    x = teste(x, t);
